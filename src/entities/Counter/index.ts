import { counterReducer } from './model/slice/counterSlice';
import { default as Counter } from './ui/Counter';
import type { CounterSchema } from './model/types/CounterSchema';

export { counterReducer, Counter, CounterSchema };
